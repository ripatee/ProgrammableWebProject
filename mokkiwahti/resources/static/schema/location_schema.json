{
    "type": "object",
    "required": ["name", "sensor_id"],
    "properties": {
        "sensor_id": {
            "description": "Sensor's unique name",
            "type": "string"
        },
        "name": {
            "description": "Name/descripton of the location",
            "type": "string"
        }
    }
}