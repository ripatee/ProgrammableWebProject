{
    "type": "object",
    "required": ["sensor_id"],
    "properties": {
        "sensor_id": {
            "description": "Sensor's unique name",
            "type": "string"
        },
        "interval": {
            "description": "Time in between measurements",
            "type": "number"
        },
        "threshold_min": {
            "description": "Lower limit of alarm threshold",
            "type": "number"
        },
        "threshold_max": {
            "description": "Upper limit of alarm threshold",
            "type": "number"
        }
    }
}